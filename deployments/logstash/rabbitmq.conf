input {
  rabbitmq {
    id => "fm-v2-logs"
    host => "rabbitmq"
    user => "guest"
    password => "guest"
    port => 5672
    vhost => "/"
    queue => "fm-v2-logs"
    ack => false
    passive	=> true
  }
}

output {
  elasticsearch {
    hosts => ["fm-v2-elasticsearch:9200"]
    index => "fm-v2-logs"
  }
}
